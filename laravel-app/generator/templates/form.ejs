<script setup lang="ts">
import { reactive } from 'vue'

type <%= componentName %>FormState = {
<% fields.forEach(function(field, index) { %>  <%= field.name %>: <%= field.type === 'checkbox' ? 'boolean' : 'string' %><%= index < fields.length - 1 ? ';' : '' %>
<% }) %>}

const form = reactive<<%= componentName %>FormState>({
<% fields.forEach(function(field, index) { %>  <%= field.name %>: <%- field.type === 'checkbox' ? 'false' : "''" %><%= index < fields.length - 1 ? ',' : '' %>
<% }) %>})

const emit = defineEmits<{
  submit: [payload: <%= componentName %>FormState]
}>()

function onSubmit() {
  emit('submit', { ...form })
}
</script>

<template>
  <form class="space-y-4 rounded-md border border-gray-200 bg-white p-4" @submit.prevent="onSubmit">
<% fields.forEach(function(field) { %>    <div class="flex items-center gap-3">
      <label for="<%= field.name %>" class="text-sm font-medium text-gray-700 min-w-32">
        <%= field.label %>
      </label>
<% if (field.type === 'checkbox') { %>      <input
        id="<%= field.name %>"
        v-model="form.<%= field.name %>"
        type="checkbox"
        class="h-4 w-4 rounded border-gray-300 text-blue-600 focus:ring-blue-500"
      />
<% } else { %>      <input
        id="<%= field.name %>"
        v-model="form.<%= field.name %>"
        type="text"
        class="w-full rounded-md border border-gray-300 px-3 py-2 text-sm focus:border-blue-500 focus:outline-none"
      />
<% } %>    </div>
<% }) %>    <div>
      <button
        type="submit"
        class="rounded-md bg-slate-900 px-4 py-2 text-sm font-medium text-white hover:bg-slate-700"
      >
        Save
      </button>
    </div>
  </form>
</template>
